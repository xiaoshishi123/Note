# JAVA视频网站部署修改

### 1，部署相关

#### （1）后端

这里后端不区本机和虚拟机，统一用下面这个项目

```
D:\JavaPractice\wisdrivideoLinux\wisdriVideoLinux
```

idea--package打包，然后将target文件夹下的jar包复制过去  



然后注意要用**虚拟机的命令行输入指令，而不是虚拟机上远程链接本机的命令行**





#### （2）前端

（1）vsdode

这里是区分的，懒得协调了

本机项目地址

```
D:\JavaPractice\wisdrivideo-vue
```

虚拟机项目地址

```
D:\JavaPractice\wisdrivideoLinux\wisdrivideovueLinux
```



因此除了正常部署 还需要**将本机的修改挨个复制到虚拟机项目**

```
ldapsearch -LLL -o ldif-wrap=no -H ldap://192.168.200.14:389 -x -D "14088@wisdri.com" -W -b "DC=wisdri,DC=com" "(sAMAccountName=65193)" cn sAMAccountName displayName department departmentNumber title mail manager distinguishedName | powershell -NoProfile -Command "$input | ForEach-Object { if($_ -match '^(?<k>[^:]+):: (?<v>.+)$'){ $v=[Text.Encoding]::UTF8.GetString([Convert]::FromBase64String($matches['v'])); \"$($matches['k']): $v\" } else { $_ } }"
```



1，继续开发用户学习内容统计模块，编写后端接口逻辑，先实现管理端的用户汇总管理功能；

2，根据同事测试反馈，整理并统计系统存在的 Bug，逐项进行排查与修复。







## 数据库迁移：



#### 1，emp_ldap_stage

过渡表  结构+数据

#### 2，emp_user

实际用户表 结构+数据



记得把BIM中心的实习生给删了

#### 3，user_video_progress    user_video_session

用户行为表  只需要结构 

#### 4，视图

可以一键迁移





## 前端迁移：

#### 1，路由route

#### 2，管理端新增的n个页面

#### 3，用户端还需考虑如何查看个人的记录



前端反正添加了一堆页面 你看着迁移吧  数不清了 



**注意还多了个心跳js  以及路由router 那俩文件挺容易忘记的**











## 🎯【短期任务目标：学习统计展示与导出】

| 序号 | 模块                 | 任务说明                                                     | 目标                                | 状态     |
| ---- | -------------------- | ------------------------------------------------------------ | ----------------------------------- | -------- |
| 1    | **后端导出功能**     | 为用户汇总（user）、视频汇总（video）、部门汇总（dept）三种统计接口分别新增 `/export` 版本，生成 Excel（xlsx），支持时间筛选、文件名带日期、日志打印 | 实现三个可直接下载 Excel 的导出接口 | ✅ 已完成 |
| 2    | **前端统计页面**     | 新建“学习统计”页面（/learn/statistics），调用后端接口并使用 ECharts 展示：① 各部门学习时长（bar）② 视频热度排行（pie）③ 用户学习排行榜（bar/line） | 在前端图形化展示学习数据            | 🔜进行中  |
| 3    | **首页入口按钮**     | 在前端首页或管理端顶部导航栏新增“学习统计”入口按钮，点击跳转至统计页面 | 页面可从主入口访问统计功能          | 待开发   |
| 4    | **导出按钮前端联动** | 在统计页面中，为每个图表增加“导出 Excel”按钮，点击后调用对应的 `/export` 接口（user/video/dept）并触发文件下载 | 可在前端直接导出对应图表数据        | 待开发   |
| 5    | **汇总数据验证**     | 手动验证汇总逻辑：① 部门统计可按日期范围过滤 ② 视频统计的总时长是否正确 ③ 用户统计的完成率逻辑是否准确 | 确保统计数据可靠                    | 待测试   |



📌 **执行顺序建议：**
 1️⃣ 先实现 **Excel 导出功能（后端）**
 2️⃣ 再开发 **前端统计页（含 3 个图表）**
 3️⃣ 最后补上 **按钮入口 + 导出联动**































本系统用于展示员工在学习平台中对不同视频分类的学习完成情况。
 从三个独立维度（**分类 → 部门 → 用户**）进行统计，每个维度对应一个独立页面。
 三个页面同级展示、互不嵌套，但可通过跳转或链接互相访问。

------

## 🧭 一、总体设计思路

| 维度  | 主体对象 | 查看内容         | 页面功能定位           |
| ----- | -------- | ---------------- | ---------------------- |
| 页面1 | 视频分类 | 各部门的完成率   | “从视频角度看组织”     |
| 页面2 | 部门     | 各分类的完成率   | “从部门角度看培训效果” |
| 页面3 | 用户     | 各分类与视频详情 | “从个人角度看学习进度” |

所有页面都具备：

- 顶部筛选区（部门 / 分类 / 时间范围）
- 表格或卡片式可视化
- 导出Excel功能
- 支持时间过滤和下钻跳转（点击跳转到另一个维度）

------

## 🧩 二、页面1：按视频分类查看各部门学习完成率

### 1️⃣ 页面目的

展示每个视频分类下，各部门的学习完成率，帮助管理者从**课程角度**了解学习覆盖率。

### 2️⃣ 页面结构

```
📊 页面1：LearnByCategory.vue
│
├── 顶部筛选区（部门、时间范围）
│
├── 分类汇总区
│     - 每个分类一个卡片
│     - 字段：分类名 / 已完成人数 / 总人数 / 完成率(%) + 进度条
│
├── 部门详情表（点击卡片展开）
│     - 部门 / 总人数 / 完成人数 / 完成率(%) / 进度条
│
└── 操作：
       - 点击部门 → 打开“页面2：按部门查看分类”
       - 支持导出Excel
```

### 3️⃣ 接口需求

| 接口                        | 方法 | 功能                 | 参数                                 |
| --------------------------- | ---- | -------------------- | ------------------------------------ |
| `/learn/summary/categories` | POST | 所有分类总体学习情况 | `startDate`, `endDate`               |
| `/learn/summary/depts`      | POST | 指定分类下的部门情况 | `categoryId`, `startDate`, `endDate` |
| `/admin/category`           | GET  | 获取所有分类         | -                                    |
| `/admin/dept`               | GET  | 获取所有部门         | 可按公司过滤                         |

### 4️⃣ 统计逻辑

- 总人数 = `emp_user` 中该部门所有员工
- 完成人数 = 完成该分类下所有视频学习的员工
- 完成率 = 完成人数 ÷ 总人数 × 100
- 视频分类 = `videos` 表按 `category_id` 聚合

------

## 🏢 三、页面2：按部门查看各分类学习完成率

### 1️⃣ 页面目的

展示单个部门在各个分类中的学习进度，从**组织角度**评估培训效果。

### 2️⃣ 页面结构

```
🏢 页面2：LearnByDept.vue
│
├── 顶部筛选区（部门选择、时间范围）
│
├── 分类统计表
│     - 分类名称 / 视频总数 / 已完成人数 / 部门总人数 / 完成率(%) / 进度条
│
└── 操作：
       - 点击分类行 → 打开“页面3：用户学习详情”
       - 导出部门学习汇总
```

### 3️⃣ 接口需求

| 接口                             | 方法 | 功能                   | 参数                             |
| -------------------------------- | ---- | ---------------------- | -------------------------------- |
| `/learn/summary/dept-categories` | POST | 某部门的各分类学习统计 | `deptId`, `startDate`, `endDate` |
| `/admin/dept`                    | GET  | 获取部门列表           | -                                |
| `/admin/category`                | GET  | 获取分类列表           | -                                |

### 4️⃣ 统计逻辑

- 统计维度：一个部门 × 多个分类
- 总人数：该部门所有员工
- 完成人数：该部门内完成该分类所有视频的员工
- 分类视频数：`videos` 中 `category_id` 分组计数
- 时间过滤同样基于 `user_video_session.started_at`

------

## 👤 四、页面3：按用户查看学习进度详情

### 1️⃣ 页面目的

展示单个用户对不同视频分类的学习完成情况，并支持查看分类下的视频学习记录。
 用于**个人进度分析**与**问题排查**。

### 2️⃣ 页面结构

```
👤 页面3：LearnByUser.vue
│
├── 顶部筛选区（工号/姓名 + 时间范围）
│
├── 分类汇总表
│     - 分类名称 / 视频总数 / 已完成 / 完成率(%) / 状态(完成/未完成)
│
├── 分类下视频明细（点击分类展开）
│     - 视频标题 / 学习时长(分钟) / 完成状态 / 最后学习时间
│
└── 操作：
       - 导出该用户学习报告
       - 支持搜索工号 / 姓名
```

### 3️⃣ 接口需求

| 接口                             | 方法 | 功能                     | 参数                              |
| -------------------------------- | ---- | ------------------------ | --------------------------------- |
| `/learn/summary/user-categories` | POST | 用户在各分类的学习情况   | `empCode`, `startDate`, `endDate` |
| `/learn/summary/user-videos`     | GET  | 用户在某分类下的学习详情 | `empCode`, `categoryId`           |

### 4️⃣ 统计逻辑

- 视频学习完成标准：`user_video_progress.completed = 1`
- 完成率 = 已完成视频 ÷ 分类总视频数 × 100
- 时间过滤：按 `user_video_session` 最近播放记录筛选
- 若无记录 → 显示 0%、灰色状态

------

## ⚙️ 五、三页面之间的关系（跳转结构）

| 来源页面        | 点击对象 | 跳转目标        |
| --------------- | -------- | --------------- |
| 页面1（按分类） | 某部门   | 页面2（按部门） |
| 页面2（按部门） | 某分类   | 页面3（按用户） |
| 页面3（按用户） | 返回按钮 | 回到来源页面    |

> 三个页面为**同级路由**，互不嵌套，跳转采用新标签或路由跳转（`router.push`）。

------

## 🧱 六、统一交互与视觉规范

| 模块     | 规范                                                       |
| -------- | ---------------------------------------------------------- |
| 筛选栏   | 统一放置部门、分类、时间范围三个条件（可根据页面禁用部分） |
| 进度条   | 按完成率动态变色（灰 / 蓝 / 绿）                           |
| 表格     | 默认带分页、排序；空数据时显示“暂无数据”                   |
| 导出     | 所有页面均支持导出当前结果为 Excel                         |
| 统计计算 | 所有接口后端计算完成率，前端只展示                         |
| 筛选刷新 | 所有查询条件变更后立即刷新数据                             |

------

## 📁 七、前端文件结构建议

```
src/views/learn-statistics/
│
├── LearnByCategory.vue     # 页面1：按视频分类看部门
├── LearnByDept.vue         # 页面2：按部门看分类
└── LearnByUser.vue         # 页面3：按用户看分类与视频
```













# 页面一：

## 🧭 一、页面目标

> 以“视频分类”为核心视角，展示各部门在该分类下所有用户的学习完成情况与总学习时长。
>  管理者可选定时间范围，快速查看部门整体学习活跃度与平均完成情况。

------

## 🧱 二、页面总体结构

```
📊 LearnByCategory.vue（重构版）
│
├── 顶部信息区
│     ├─ 分类选择（下拉框）
│     ├─ 时间范围选择（开始时间 - 结束时间）
│     ├─ 查询 / 重置按钮
│
├── 部门汇总表（主要展示区域）
│     ├─ 字段：
│         部门名称 | 总人数 | 已完成人数 | 完成率(%) | 学习总时长(h) | 平均时长(h)
│     ├─ 支持排序、导出Excel
│     ├─ 点击部门行 → 展开部门成员明细
│
├── （可展开）部门成员学习详情表
│     ├─ 字段：
│         姓名 | 工号 | 学习时长(h) | 已完成/总视频数 | 完成率(%) | 状态(完成/未完成)
│     ├─ 默认折叠，仅点击部门行展开
│
└── （页面底部）时间段统计总览
      - 展示当前分类在所选时间段内：
        【总学习时长】、【平均学习时长】、【总完成人数】、【总体完成率】
```

------

## 🧩 三、数据展示逻辑

| 指标         | 计算逻辑                                            | 来源                |
| ------------ | --------------------------------------------------- | ------------------- |
| 总人数       | 该部门在 emp_user 表中的人数                        | emp_user            |
| 已完成人数   | 在该分类下所有视频均完成的用户数                    | user_video_progress |
| 完成率       | 已完成人数 ÷ 总人数 × 100                           | -                   |
| 学习总时长   | 所选时间范围内该部门所有用户观看的总秒数求和 / 3600 | user_video_session  |
| 平均时长     | 学习总时长 ÷ 部门总人数                             | -                   |
| 用户完成率   | 用户在该分类下完成视频数量 ÷ 分类视频总数 × 100     | user_video_progress |
| 用户学习时长 | 所选时间段内观看总秒数                              | user_video_session  |

------

## ⚙️ 四、接口规划（共 3 个）

| 接口                                  | 方法 | 功能                                       | 参数                                             |
| ------------------------------------- | ---- | ------------------------------------------ | ------------------------------------------------ |
| `/learn/summary/depts-with-time`      | POST | 某分类下各部门学习情况（含学习时长）       | `categoryId`, `startDate`, `endDate`             |
| `/learn/summary/dept-users-with-time` | POST | 某部门下用户学习详情（含完成率、学习时长） | `categoryId`, `deptName`, `startDate`, `endDate` |
| `/admin/category`                     | GET  | 获取所有分类                               | —                                                |

> 💡 建议你新建一个 Service / Mapper 组合，比如：
>
> - Mapper：`ProgressTimeMapper.xml`
> - Service：`ProgressTimeService.java`
> - Controller：`ProgressTimeController.java`























